{#
<div class="modal fade" id="crearModal" tabindex="-1" role="dialog" aria-labelledby="tituloModal" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            {{ form_start(casoConciliatorio, {'action': url}) }}
            <div class="modal-header"> <!-- MODAL CABECERA -->
                <h5 class="modal-title" id="tituloModal">Modal Nueo</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Cerrar">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body"> <!-- MODAL CUERPO -->
                {{ form_row(caso.fecha, {'label': 'Fecha de Regis'}) }}
                {{ form_row(caso.materia, {'label': 'Tipo de materia'}) }}
                {{ form_row(caso.tipoConciliacion, {'laabel': 'Tipo de conci'}) }}
            </div>
            <div class="modal-footer"> <!-- MODAL PIE -->
                <button type="button" class="btn btn-default" data-dismiss="modal">Cancelar</button>
                <button type="submit" class="btn btn-primary">Guardar</button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
#}